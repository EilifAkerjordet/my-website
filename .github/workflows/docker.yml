name: Create and push Docker images

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: make nextjs .env.local
        uses: SpicyPizza/create-envfile@v1
        with:
          envkey_API_URL: ${{ secrets.API_URL }}
          file_name: ./nextjs_app/.env.local
      - name: docker login
        env:
          DOCKER_USER: ${{ secrets.DOCKER_USER }}
          DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
        run: |
          docker login -u $DOCKER_USER -p $DOCKER_PASSWORD
      - name: docker build
        run: |
          docker build -f ./strapi_cms/Dockerfile.prod ./strapi_cms -t eilifakerjordet/personal-web-strapi:1.0.0
      - name: docker build next.js
        run: |
          docker build -f ./nextjs_app/Dockerfile.prod ./nextjs_app -t eilifakerjordet/personal-web-nextjs:1.0.0
      - name: docker build nginx
        run: |
          docker build -f ./nginx/Dockerfile.prod ./nginx -t eilifakerjordet/personal-web-nginx:1.0.0
      - name: docker push
        run: |
          docker push eilifakerjordet/personal-web-strapi:1.0.0
          docker push eilifakerjordet/personal-web-nextjs:1.0.0
          docker push eilifakerjordet/personal-web-nginx:1.0.0
